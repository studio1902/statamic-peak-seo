title: 'Globals SEO: JSON-ld - Knowledge Graph'
fields:
  -
    handle: json_ld_type
    field:
      options:
        none: None
        organization: Organization
        person: Person
        custom: Custom
      default: none
      localizable: true
      type: button_group
      instructions: 'The type of data to be added on each page.'
      listable: false
      display: Type
  -
    handle: organization_name
    field:
      type: text
      localizable: true
      display: 'Organization name'
      width: 50
      if:
        json_ld_type: 'equals organization'
      validate:
        - sometimes
        - required
  -
    handle: organization_alternate_name
    field:
      type: text
      localizable: true
      display: 'Organization alternate name'
      width: 50
      if:
        json_ld_type: 'equals organization'
  -
    handle: organization_description
    field:
      type: text
      localizable: true
      display: 'Organization description'
      if:
        json_ld_type: 'equals organization'
  -
    handle: organization_logo
    field: common.image
    config:
      localizable: true
      listable: hidden
      display: 'Organization logo'
      instructions_position: below
      instructions: 'Minimum 112px x 112px, will be focal cropped to a square.'
      width: 50
      if:
        json_ld_type: 'equals organization'
      show_set_alt: false
  -
    handle: person_name
    field:
      display: 'Person name'
      width: 50
      type: text
      localizable: true
      if:
        json_ld_type: 'equals person'
      validate:
        - 'required_if:json_ld_type,person'
  -
    handle: json_ld
    field:
      instructions: 'Add custom JSON-LD to be included on each page. This can include stuff like business hours and contact data. You can use Antlers. The snippet will be wrapped in the appropriate script tag.'
      localizable: true
      listable: false
      antlers: true
      mode: application/ld+json
      mode_selectable: false
      show_mode_label: false
      type: code
      display: JSON-LD
      if:
        json_ld_type: 'equals custom'
      validate:
        - 'required_if:json_ld_type,custom'
